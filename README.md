# Учебный блог на Django

Этот проект - это учебное веб-приложение, созданное на Django.  
В нём реализованы три страницы: главная страница с выводом постов, страница «О блоге» и страница просмотра отдельного поста.  
Проект использует систему шаблонов Django, наследование от базового шаблона и статические файлы (CSS и изображения).

---

# 📌 Установка и запуск проекта с нуля

Ниже приведена подробная инструкция по установке и запуску проекта.  
Для выполнения шагов требуется только установленный интерпретатор **Python**.

---

## 1. Скачивание проекта

Скачайте проект с GitHub (ZIP-архив или через git):

```bash git clone https://github.com/Kolnievbfh/blog_for_technologies_for_creating_internet_applications.git```

Затем перейдите в директорию проекта:
cd ВАШ_РЕПОЗИТОРИЙ

## 2. Создание виртуального окружения
Создайте виртуальное окружение Python:
python -m venv venv
## 3. Активация виртуального окружения
▶ Windows
venv\Scripts\activate

▶ macOS / Linux
source venv/bin/activate

После активации вы увидите префикс (venv) в начале строки терминала.

## 4. Установка зависимостей
Установите необходимые библиотеки:
pip install -r requirements.txt

Файл requirements.txt содержит фиксированные версии всех использованных зависимостей.

## 5. Применение миграций базы данных
Django использует встроенную SQLite-базу, поэтому дополнительная установка не требуется.

Запустите миграции:
python manage.py migrate
## 6. Запуск сервера разработки
Для запуска локального сервера выполните:
python manage.py runserver

Если команда выполнена успешно, сервер будет доступен по адресу:

http://127.0.0.1:8000/

## 📝 Инструкция по работе с системой отзывов (UPD)

### 1. Добавление нового отзыва пользователем

1. Перейдите на страницу отзывов:
http://127.0.0.1:8000/reviews/

2. Заполните форму:
- ФИО
- Email
- Текст отзыва
3. Нажмите **«Отправить»**.

Отзыв будет сохранён, но **не появится сразу на странице**, потому что он требует проверки.

---

### 2. Проверка отзывов администратором

1. Перейдите в административную панель:
http://127.0.0.1:8000/admin/

2. Авторизуйтесь под суперпользователем  
*(создать суперпользователя можно командой `python manage.py createsuperuser`)*.
3. В разделе **Reviews** откройте список отзывов.
4. Выберите нужный отзыв.
5. Поставьте галочку **"Проверено"**.
6. Нажмите **Save**.

После этого отзыв появится в списке на странице `/reviews/`.

---

### 3. Почему отзыв не отображается сразу?

Это сделано специально:

- чтобы защищаться от спама
- чтобы администратор мог отклонять нежелательные сообщения
- чтобы пользователь не мог отметить свой отзыв как проверенный

Поэтому поле **checked=False** по умолчанию.

---

### 4. Как удалить или отредактировать отзыв?

1. Войдите в админку (`/admin/`).
2. Перейдите в раздел **Reviews**.
3. Нажмите на отзыв.
4. Нажмите:
- **Delete** — чтобы полностью удалить
- измените текст и нажмите **Save** — чтобы отредактировать

---

### 5. Как выглядит процесс для обычного пользователя?

- Он видит только **проверенные** отзывы.
- Он может отправить новый отзыв через форму.
- После отправки ему не показывают “модерацию”, но отзыв исчезает до проверки.
- Админ решает, появится отзыв на странице или нет.

---

### 6. Как выглядит процесс для администратора?

- Он видит все отзывы (и проверенные, и нет).
- Он решает, какой опубликать.
- Он может редактировать и удалять отзывы.
- В админке все новые отзывы помечены как “Проверено = Нет”.

---