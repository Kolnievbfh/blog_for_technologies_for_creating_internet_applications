# Учебный блог на Django

Данный проект представляет собой простое учебное веб-приложение, созданное на Django.  
В нём реализованы три страницы: главная страница с выводом постов, страница «О блоге» и страница просмотра отдельного поста.  
Проект использует систему шаблонов Django, наследование от базового шаблона и статические файлы (CSS и изображения).

---

# 📌 Установка и запуск проекта с нуля

Ниже приведена подробная инструкция по установке и запуску проекта.  
Для выполнения шагов требуется только установленный интерпретатор **Python**.

---

## 1. Скачивание проекта

Скачайте проект с GitHub (ZIP-архив или через git):

```bash git clone https://github.com/Kolnievbfh/blog_for_technologies_for_creating_internet_applications.git```

Затем перейдите в директорию проекта:
cd ВАШ_РЕПОЗИТОРИЙ

## 2. Создание виртуального окружения
Создайте виртуальное окружение Python:
python -m venv venv
## 3. Активация виртуального окружения
▶ Windows
venv\Scripts\activate

▶ macOS / Linux
source venv/bin/activate

После активации вы увидите префикс (venv) в начале строки терминала.

## 4. Установка зависимостей
Установите необходимые библиотеки:
pip install -r requirements.txt

Файл requirements.txt содержит фиксированные версии всех использованных зависимостей.

## 5. Применение миграций базы данных
Django использует встроенную SQLite-базу, поэтому дополнительная установка не требуется.

Запустите миграции:
python manage.py migrate
## 6. Запуск сервера разработки
Для запуска локального сервера выполните:
python manage.py runserver

Если команда выполнена успешно, сервер будет доступен по адресу:

http://127.0.0.1:8000/